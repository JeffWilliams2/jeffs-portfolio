---
layout: '../../layouts/Page.astro'
title: Cards
---

import PostCard from '../../components/PostCard.astro';
import PostCardMore from '../../components/PostCardMore.astro';
import PostCardSmall from '../../components/PostCardSmall.astro';

import ProjectCard from '../../components/ProjectCard.astro';

import { getAllPostsWithReadingTime } from '../../modules/post/common';
import { getAllProjects } from '../../modules/project';

back to [Index](/design)

export const posts = await getAllPostsWithReadingTime();
export const post = posts[0] ?? null;
export const threePosts = posts.slice(0, 3);

export const postWithoutImage = post ? {
  ...post,
  data: {
    ...post.data,
    noHero: true,
  },
} : null;

export const projects = await getAllProjects();
export const project = projects[0];
export const threeProjects = projects.slice(0, 3);

## PostCard

<div class="not-prose">
  {post ? <PostCard post={post} /> : <p>No posts available</p>}
</div>

---

## PostCard without Image

<div class="not-prose">
  {postWithoutImage ? <PostCard post={postWithoutImage} /> : <p>No posts available</p>}
</div>

---

## PostCardMore

<div class="not-prose flex gap-4">
  {threePosts.map((post) => (
    <PostCardMore post={post} />
  ))}
</div>

---

## PostCardSmall

<div class="not-prose">
  {threePosts.map((post) => (
    <PostCardSmall post={post} />
  ))}
</div>

---

## ProjectCard

<div class="not-prose flex gap-4">
  {threeProjects.map((project) => (
    <ProjectCard project={project} />
  ))}
</div>
